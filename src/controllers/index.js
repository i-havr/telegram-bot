const {
  testController,
  autoSubscribe,
  startCommenting,
} = require("./subscribe");

const {
  loginUserController,
  logoutUserController,
  currentUserController,
} = require("./usersController");

module.exports = {
  testController,
  autoSubscribe,
  startCommenting,
  loginUserController,
  logoutUserController,
  currentUserController,
};
